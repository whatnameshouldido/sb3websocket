server:
  port: 8089
  servlet:
    encoding:
      charset: UTF-8
      enabled: true
      force: true

spring:
  application:
    name: sb3websocket

  # mustache
  mustache:
    enabled: true
    charset: UTF-8
    suffix": .html
    check-template-location: true
